version: '3.8'

services:
  easyrun-api:
    build: .
    container_name: easyrun-api
    ports:
      - "8000:8000"
    environment:
      - API_VERSION=1.0.0
      - REQUEST_TIMEOUT=30.0
      - OVERPASS_URL=https://overpass-api.de/api/interpreter
      - ELEVATION_URL=https://api.open-elevation.com/api/v1/lookup
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173
      - CACHE_SIZE_OSM=50
      - CACHE_SIZE_ELEVATION=100
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - easyrun-network

  # Optional: PostgreSQL for data persistence (future)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: easyrun-db
  #   environment:
  #     - POSTGRES_USER=easyrun
  #     - POSTGRES_PASSWORD=changeme
  #     - POSTGRES_DB=easyrun
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - easyrun-network

  # Optional: Redis for distributed caching (future)
  # redis:
  #   image: redis:7-alpine
  #   container_name: easyrun-cache
  #   networks:
  #     - easyrun-network

networks:
  easyrun-network:
    driver: bridge

# volumes:
#   postgres_data:
